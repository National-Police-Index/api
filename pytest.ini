[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: hits live Supabase and/or a running API server (opt-in; deselect with -m "not integration")
addopts = -m "not integration"
